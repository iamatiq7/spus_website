{
  "name": "spus-website-server",
  "version": "1.0.0",
  "private": true,
  "description": "Optional production backend for Shantinagar Polli Unnayan Somiti website (static preview cannot run servers).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
